# Frontend environment variables used by ParlanceChat

REACT_APP_BACKEND_URL=http://localhost:8001 # Backend proxy URL for API requests and WebSocket connections
ENABLE_HEALTH_CHECK=false # Enable health-check plugin logic if needed
NODE_ENV=development # Build environment mode used by React and some plugins
