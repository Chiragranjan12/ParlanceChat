# Backend proxy environment variables for ParlanceChat

FRONTEND_URL=http://localhost:3000 # Allowed frontend origin for CORS in the proxy server
REACT_APP_BACKEND_URL=http://localhost:8001 # Used by backend tests to target the running frontend/backend stack
